global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]

  # Add your Kubernetes / kube-state-metrics / node-exporter targets below.
  # Example:
  # - job_name: "kubernetes-nodes"
  #   kubernetes_sd_configs:
  #     - role: node
